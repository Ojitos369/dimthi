export const PageHeader = ({ ls }) => {
    return (
        <div className={ls.style.pageHeader}>
            <div className={ls.style.title}>Catálogo de Cotizaciones</div>
        </div>
    );
};
